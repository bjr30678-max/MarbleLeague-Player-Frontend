# LINE LIFF Configuration
VITE_LIFF_ID=your-liff-id-here

# API Configuration
# Development: Leave empty or use http://localhost:3000 to use Vite proxy (bypasses CORS)
# Production: Use full API URL like https://api.example.com
VITE_API_URL=https://api.example.com

# Environment
# development: 使用模擬 LIFF，啟用開發模式訊息，使用 Vite proxy
# production: 使用真實 LIFF，連接真實 API
VITE_ENV=production

# AWS IVS Configuration (Optional - uses VITE_API_URL if not specified)
# VITE_AWS_IVS_API_URL=https://api.example.com
# VITE_AWS_IVS_API_KEY=your-api-key-here

# Live Stream Configuration (Optional)
# VITE_STREAM_HOST=your-stream-host
# VITE_STREAM_APP=live
# VITE_STREAM_NAME=stream
